# Database Configuration
# IMPORTANT: Do NOT commit real credentials here.
# Set these as environment variables or use a local config file outside version control.
db.url=jdbc:postgresql://localhost:5432/bookingsystemdb
db.username=postgres
db.password=CHANGE_ME
db.driver=org.postgresql.Driver

# HikariCP Pool Settings
db.pool.maximumPoolSize=10
db.pool.minimumIdle=2
db.pool.idleTimeout=30000
db.pool.connectionTimeout=30000
db.pool.maxLifetime=600000
